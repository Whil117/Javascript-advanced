const name = "Ivan"
const last_name = "Garcia"

const fullName = `${name} ${last_name} `

function getSalute(name) {
    return `Hola ${name}`
}
console.log(`Diganle un ${getSalute(fullName)}`)